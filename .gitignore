# virtual env
.venv/
venv/

# python
__pycache__/
*.pyc
.pytest_cache/

# app data: sqlite db, stored thumbnails, runtime config
data/

# downloaded sample/test images (kept out of the repo)
samples/

# InsightFace downloads its models here on first run
.insightface/

# cached embeddings from the threshold sweep (the PNGs are committed)
eval/lfw_embeddings.npz

# editor / tooling junk
.vscode/
.idea/
.DS_Store
.claude/
